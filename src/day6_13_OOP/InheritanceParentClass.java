package day6_13_OOP;

public class InheritanceParentClass {
	 String name;
	 int age;

    void displayInfo() {
        System.out.println("User name is:" + name);
        System.out.println("User age is:" + age);
    }
}


/*
 
 * Inheritance that allows a class (subclass or derived class) to inherit the properties and behaviors of another class 
 * (parent class, superclass or base class). In Java, inheritance is implemented using the extends keyword.
 * Why Inheritance> for code Reusability, extensibility, method overriding, and the creation of a hierarchy of classes
 */
